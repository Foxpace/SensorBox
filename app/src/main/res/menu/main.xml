<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/wear_os_action_sync"
        android:icon="@drawable/ic_wear_os_download_data"
        android:title="@string/sync_data"
        android:enabled="false"
        android:visible="false"
        app:showAsAction="always"/>

    <item
        android:id="@+id/wear_os_action_present"
        android:icon="@drawable/ic_wear_os_off"
        android:title="@string/wear_os_presence"
        android:enabled="false"
        android:visible="false"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />

    <!-- Settings, should always be in the overflow -->

</menu>